<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ScriptSphere</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Segoe UI", sans-serif;
        background-color: #121212;
        color: #e0e0e0;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      header {
        background-color: #1f1f1f;
        padding: 20px;
        text-align: center;
      }

      header h1 {
        margin-bottom: 5px;
        color: #ffffff;
      }

      nav {
        background-color: #2c2c2c;
        padding: 10px;
        text-align: center;
        align-items: center;
      }

      nav a {
        color: #90caf9;
        margin: 0 15px;
        text-decoration: none;
        font-weight: bold;
      }

      nav a:hover {
        text-decoration: underline;
      }

      .container {
        flex: 1;
        max-width: 900px;
        margin: 20px auto;
        background-color: #2c2c2c;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
      }

      .user-info {
        display: flex;
        align-items: center;
        gap: 20px;
        margin-top: 15px;
        border-bottom: 2px solid white;
      }

      .user-info img {
        width: 120px;
        height: 120px;
        border-radius: 50%;
        object-fit: cover;
        border: 2px solid #90caf9;
        margin-bottom: 15px;
      }

      .user-details p {
        margin-bottom: 8px;
      }

      footer {
        background-color: #1f1f1f;
        color: #aaa;
        text-align: center;
        padding: 15px;
        font-size: 14px;
      }

      body {
        margin: 0;
        padding: 0;
        font-family: "Segoe UI", sans-serif;
        background-color: #121212;
        color: #ffffff;
      }

      .blog-section {
        min-width: 500px;
        max-width: 800px;
        margin: 20px auto;
        padding: 20px;
      }

      .blog {
        background-color: #1f1f1f;
        margin-bottom: 20px;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.5);
      }

      .blog h3 {
        margin-top: 0;
        color: #ffffff;
      }

      .blog h4 {
        margin-top: 0;
        color: #90caf9;
      }

      .blog p {
        line-height: 1.6;

        color: #ddd;
      }

      #content {
        line-height: 1.4;
        color: #ddd;
        word-wrap: break-word;
        overflow-wrap: break-word;
      }

      .blog time {
        font-size: 0.9em;
        color: #999;
        display: block;
      }

      .users {
        display: flex;
        justify-content: center;
        align-items: center;
        cursor: pointer;
        color: white;
        margin-top: 15px;
        right: 0;
        top: -12px;
      }

      .users a {
        text-decoration: none;
        font-size: 18px;
        padding: 5px;
        background-color: #dc2626;
        border-radius: 8px;
        border: none;
        color: white;
      }

      .users a:hover {
        text-decoration: none;
      }

      .blogpost {
        display: flex;
        justify-content: center;
        align-items: center;
        cursor: pointer;
        color: white;
      }

      .blogpost a {
        text-decoration: none;
        font-size: 18px;
        padding: 8px;
        background-color: #1e40af;
        border-radius: 8px;
        border: none;
        color: white;
      }

      img {
        height: 37px;
        width: 39px;
        border-radius: 50%;
        background-position: center;
        background-repeat: no-repeat;
        background-size: cover;
      }
    </style>
  </head>

  <body>
    <header>
      <div>
        <h1>ScriptSphere</h1>
        <p>write some blogs</p>
      </div>
    </header>

    <nav style="position: relative">
      <a href="/profile">Profile</a>
      <a href="/home">Home</a>
      <a href="/login">login</a>
      <a href="/users">Users</a>
      <div class="users" style="position: absolute">
        <a href="/logout">Logout</a>
      </div>
    </nav>

    <div class="container">
      <div class="blogpost"><a href="/blogpost">Write you blog</a></div>

      <div class="blog-section">
        <% blogs.reverse().forEach(element=> { %>
        <div class="blog">
          <div
            style="
              display: flex;
              gap: 16px;
              margin-bottom: 10px;
              align-items: center;
            "
          >
            <img
              style="background-position: center"
              src="/img/profile/<%= user.image %>"
              alt="img"
            />
            <h4 style="cursor: pointer"><%= element.user %></h4>
          </div>
          <h3><%= element.title %></h3>

          <p id="content"><%= element.content %></p>
          <small style="color: #dc2626">
            <%= element.likes.length %> Likes
          </small>
          <div style="display: flex; gap: 14px; align-items: center">
            <p>
              <a
                style="color: #2a52d6; cursor: pointer; text-decoration: none"
                href="/like/<%= element._id %>/<%= user._id %>"
              >
                <%= element.likes.indexOf(user._id)===-1 ? 'like' : 'unlike' %>
              </a>
            </p>
            <time datetime="2025-05-01">
              <%= element.date.toDateString() %>
            </time>
          </div>
        </div>
        <% }); %>
      </div>
    </div>

    <footer>&copy; 2025 MySite. All rights reserved.</footer>
  </body>
</html>
